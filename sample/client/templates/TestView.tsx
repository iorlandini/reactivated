import React from 'react';

import {TestProps as Props} from '../models';

/*
import {style} from 'typestyle';


class Styles {
    static readonly postTitle = style({
        marginBottom: 0,
    });

    static readonly body = style({
    });
}
*/


export default (props: Props) => {
    return <div>
        Hello
    </div>
}
